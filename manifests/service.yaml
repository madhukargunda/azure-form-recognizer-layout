apiVersion: v1
kind: Service
metadata:
  name: layout-form-recognizer
  labels:
    app.kubernetes.io/name: form-recognizer
    app.kubernetes.io/instance: layout
    app.kubernetes.io/version: "3.1"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: form-recognizer
    app.kubernetes.io/instance: layout
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP

