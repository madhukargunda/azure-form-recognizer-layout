1. The Form Recognizer Layout service is deployed as:
   {{ include "form-recognizer.fullname" . }}

2. To access the service inside the cluster:
   kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "form-recognizer.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.targetPort }}

3. If you use a non-ClusterIP service, check your Service configuration for external access.

