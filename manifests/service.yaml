apiVersion: v1
kind: Service
metadata:
  name: azure-docintel-layout
  labels:
    app.kubernetes.io/name: azure-docintel-layout
    app.kubernetes.io/instance: default
    app.kubernetes.io/version: "3.1"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: azure-docintel-layout
    app.kubernetes.io/instance: default
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP

